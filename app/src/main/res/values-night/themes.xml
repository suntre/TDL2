<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ToDoList" parent="Theme.MaterialComponents.Light">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary_bg_color</item>
        <item name="colorPrimaryVariant">@color/primary_light_bg_color</item>
        <item name="colorOnPrimary">@color/primary_text_color</item>
        <!-- Secondary sbrand color. -->
        <item name="colorSecondary">@color/secondary_bg_color</item>
        <item name="colorSecondaryVariant">@color/secondary_bg_color</item>
        <item name="colorOnSecondary">@color/secondary_text_color</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
</resources>